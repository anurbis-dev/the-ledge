# Context map — the LEDGE

Пиксельный платформер 320×180, тайл 16px. Канон: `tmp/ledge-v19.html`.

## Точки входа

- `index.html` — DOM (canvas `#c`, меню, стик, кнопки, редактор)
- `src/main.js` — bootstrap
- `src/core/game.js` — фасад симуляции (бывший IIFE `GAME`)
- `src/app/loop.js` — rAF, камера, пауза, меню

## Ядро

| Модуль | Суть |
|--------|------|
| `core/constants.js` | `T=16`, ids тайлов, физика `C` |
| `core/runtime.js` | `MAP_W/H`, `base` Uint8Array, `LV`, `W` |
| `core/map.js` | запросы тайлов, скосы, `rectFree` |
| `core/player.js` | хват кромки, лаз, лестницы, перекладины, вода, авто-карабканье на уступ в 1 тайл (`tryMantle`) |
| `core/step.js` | один тик мира; на скосе `dx` режется на `SLOPE_ALONG/√2` (~90% бега вдоль склона) |
| `levels/` | 5 карт, `build()` пишет в `base` |
| `entities/` | mk/step врагов, лифтов, факелов, сундуков, щупалец |
| `entities/ids.js` | `findById` / `allocId` — двери и объекты по id, не по индексу |

Двери: `pair` / `doorId` / `warp.to` — это `id`, не индекс массива. Факел в руке — `p.torch` = id.

Финиш 5-го уровня рисует outro; меню открывается после касания/пробела (`next: -1`).

Прыжок с поверхности воды: `swimLaunch` только вверх; на входе/возврате — короткий нырок (~тайл), потом всплытие. Удержание вниз по-прежнему ныряет глубже. В воде `apexY` не копится — всплытие не даёт урон как с падения. С поверхности к сухому берегу + направление — `tryClimbOut` (лаз или хват); под водой и по подводным полкам не вылазиваемся.

Уступ в 1 тайл при ходьбе — `tryMantle` → `startVault`, `p.climb.kind==='vault'`, факел не роняется и скорость `vx` сохраняется (не хват за кромку, отдельная короткая поза). Мантия только из стойки (`stance===0` и не из приседа/лёжа в том же кадре). Из свисания лаз сразу в стойку щели (`bestLand`: стоя / присед / лаз). Авто-подъём (`gapCrawl`) — только после щели, где встать было нельзя (из лёжа или приседа); в открытом месте перед щелью стойку не поднимаем. У открытого края ↓ по нажатию: присед → лёжа → вис; удержание ↓ (`C.EDGE_HOLD`) — сразу в вис, кроме хода от края (`awayFromEdge`) — тогда лаз лёжа, не спуск. `tryCrawlEdge` встаёт сам только если `gapCrawl` (выход из щели на губу); иначе стоп, ↓ решает.

Щупальца водорослей (`entities/tendrils.js`, `LV.tendrils: [col, row, kind]` или `[col, row, kind, side]`): корень на твёрдом тайле. `side` 0 пол↑, 1 потолок↓, 2 левая стена→, 3 правая стена← (без поля — пол). kind 0 тянется и жалит (−1 hp); kind 1 обвивает (`p.state==='snare'`), тащит к корню, держит `C.TEND_HOLD` (~2.6 с) и отпускает. Воздух на хвате тикается (`tickSnareAir`); вырваться — палкой, гарпуном или дёрганьем. Убийство палкой/гарпуном — в `stepTendrils` (`probeHarpoons`, цикл импорта через `harpoons.js` нельзя). Рисуются поверх героини (`tendrils()`).

Пауки (`entities/spiders.js`): если потолок над ними пропадает (хрупкий `CRUMB` в `S.gone`) — падают и убегают (`fall`/`flee`, без лута). Паутину на спуске можно порвать палкой или гарпуном — тот же срыв. Нить рисуется чередованием светлого/тёмного, не сплошной верёвкой.

Подбор факела/палки (`tryAction` в `entities/torches.js`) — `tryAction` только ставит `p.pickPend`/`p.pickT`; фактический attach (`t.held`/`p.torch`/`giveGear`) делает `resolvePickup` (вызывается из `step.js` каждый кадр) на нижней точке приседа (доля `PICK_APEX=0.45`, синхронно с `render/poses.js:pickPose`). Бросок факела симметрично: `p.throwT`/`p.throwPend`, сам `dropTorch(S,true)` срабатывает в `resolvePickup` на доле `THROW_REL=0.4` (синхронно с `throwPose`). Ничего не блокирует управление — это оверлей позы поверх `state==='normal'`.

Стойка стоя/присед/лёжа: хитбокс меняется мгновенно в `setStance` (иначе физика/коллизии рассинхронятся), но только для сознательного переключения по вводу (блок в `step.js` вокруг `stanceBefore`) заводится `p.stanceT`/`p.stanceFrom` — короткий (`C.STANCE_T`) блендинг позы в `render/hero.js:boxPose`. Защитные вызовы `setStance` (лифт, потолок, после переката) хитбокс тоже меняют мгновенно, но блендинг не запускают — не порождают паразитных анимаций.

Падение: `fall > C.SAFE` без переката — `startFallRecover` в присед (`C.FALL_CROUCH_T`) или лёжа если `fall > C.HURT` (`C.FALL_PRONE_T`). `p.recoverSt` держит стойку, `state==='stun'` блокирует ввод; по таймеру `finishFallRecover` сама встаёт (или в присед, если потолок), ещё `C.STANCE_T` без управления, потом контроль. Урон как раньше. `hp<=0` — `S.dead`, игра на паузе, затемнение и плашка «life capacity reached»; тап/любая клавиша — в меню (больше не `resetPlayer` на чекпоинт).

Взгляд вверх стоя на месте: `p.lookUp` (0..1, плавно) в `step.js`, поза `LOOKUP_A`. Камера уже давно приподнимается в этом случае (`wantLook` в `loop.js`) — только позы игроку не хватало.

## Рендер

Процедурные пиксели (`rc`/`lb` + кэш `fillStyle`), кэш чанков 8×8. Свет — offscreen multiply; мерцание факела — альфа, не радиус. Позы героя — скелетные ключи. Волны воды — полоса на кадр, только на настоящей поверхности (не под камнем внутри воды); передний план — offscreen по ширине карты. `torchPts` кэшируется на кадр. Камера — `clampCam` + мёртвая зона (`cam.ax/ay`, `loop.js`): цель камеры (`want`) следует не за игроком напрямую, а за якорем, который двигается только когда игрок выходит за 10×8px зону вокруг него; сама `cam.x/y` по-прежнему eased к цели каждый кадр. Приземление (`landdust`, `fall>6`) — мягкая пыль у ног, число/разлёт от высоты; удар головой о потолок в прыжке (`bonk` в `moveY`, `vy<-36`) — крошка вниз. Частицы `kind:'dust'` в `drawParts` (альфа, размер, drag).

## Ввод

Клавиши + виртуальный стик слева (`#stick`) + ✋/⤒ справа. Редактор — нижняя панель `#edbar`.

## Не путать

- `D:\TMP\AI_games\levelDesigner` — редактор уровней, не эта игра.
- `D:\TMP\AI_games\Ledge_game` — старый экспорт проекта редактора.
- Порт в редактор (`LEDGE_PORT_PLAN`) — отдельная линия, этот репо автономный.
