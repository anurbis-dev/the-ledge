# Changelog (unreleased)

- Перекат: спрайт крутится целиком (`rollAng`), голова больше не рисуется отдельно с фиксированной ориентацией.
- Героиня: убран контровой блик от факелов (`lightDirAt` / rim).

