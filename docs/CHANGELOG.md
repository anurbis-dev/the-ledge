# Changelog (unreleased)

- Убран 1px морг fold-окон при закрытии inventory/intro/outro: мелкие кадры fold-блиита теперь не рисуются.
- После закрытия inventory больше не вспыхивает панель паузы (один кадр `drawPaused` после `stepInv`).
- Узкая фаза fold больше не сжимает панель в 2px-шум: вместо блита рисуется чистая складка.
