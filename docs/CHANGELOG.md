# Changelog (unreleased)

- Редактор: RMB стирает `Door` (оба конца пары), `Exit`, `Start` (сброс) и прочие specials; индикация пар дверей (цвет/бейдж + линия); запрет ставить тот же `kind` повторно в одну клетку.
