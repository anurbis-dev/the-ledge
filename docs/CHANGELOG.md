# Changelog (unreleased)

- Тайлы и персонажи в одном спрайт-окне: встроенный тайл можно перекрасить (коллизия заводская); у героини/врагов — строка кадров на каждую анимацию, клик открывает кадр в пиксельном редакторе. Неправленный кадр в игре остаётся старым рисунком.
