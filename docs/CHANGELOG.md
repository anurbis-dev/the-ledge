# Changelog (unreleased)

- Подбор с стоя: хитбокс `idle→crouch→idle` около апекса `pickPose` (не на всём `pickT`); ноги якорятся к низу box при смене `h` — без провала в пол и без «высокой» уязвимости в приседе.
