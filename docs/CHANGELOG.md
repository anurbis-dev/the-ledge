# Changelog (unreleased)

- Fix(core): у 'chest' в defaults.js осталась левая от незавершённого прошлого захода `_meta` override `fw/fh=24` (полтора тайла) — из-за неё сундук после подключения спрайта в этой сессии рендерился заметно крупнее сетки уровня. Сброшено до штатных 16×16 (`oy` соответственно назад к 16, флеш к полу) через `setSpriteSize` + Bake. Заодно уменьшен каталожный footprint 'boulder' с 16×16 до 13×13 — ближе к его реальному хитбоксу (`BW=12,BH=11` в entities/boulders.js), иначе новый спрайт-рендер (`blitCenteredRotSprite`) рисовал камень заметно крупнее его физической коллизии.
- Fix(core): та же категория бага найдена ещё в двух местах и тоже сброшена до штатного размера через Bake — `key` (24×24 → 16×16, тот же незавершённый прошлый заход, что и у chest) и кастомный тайл `stoneBluesh`/tile 66 (`s_9`/`s_14`, footprint `_meta` был 32×32 при заявленных в SPRITE_DEFS 16×16 — тайл занимал на карте площадь 2×2 клетки вместо одной). `coin` отдельно перепроверен и не является багом — его footprint 8×8 (арт 16×16 даунскейлится 2:1), то есть меньше тайла, а не больше.
